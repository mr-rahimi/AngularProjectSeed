import { Component, OnInit, OnDestroy, QueryList, ViewChildren } from '@angular/core';
import { BookListService } from '../book-list.service';
import { {{pascalCase entity.name}}ModuleService } from './service.module';

@Component({
  selector: '{{camelCase entity.name}}-list',
  templateUrl: './{{camelCase entity.name}}-list.component.html',
  styleUrls: ['./{{camelCase entity.name}}-list.component.scss'],
})
export class {{pascalCase entity.name}}ListComponent implements OnInit, OnDestroy {

  constructor(
    public service: {{pascalCase entity.name}}ModuleService
  ) {
  }
  onPageChange(event: number) {
    this.service.pagingOptions.pageIndex = event - 1;
    this.service.refresh();
  }
  onPageSizeChange() {
    this.service.refresh();
  }
  ngOnDestroy(): void {
  }
  ngOnInit() {
    this.service.refresh();
  }
}
